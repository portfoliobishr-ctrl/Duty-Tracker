import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { ToastProvider } from "@/context/ToastContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DutyTracker • Student Duty & Prayer Rotation System",
  description: "Minimalist, modern duty tracking system for cooking and congregational prayers",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-slate-50/80 text-slate-900 font-sans selection:bg-emerald-100 selection:text-emerald-900">
        <ToastProvider>
          {children}
        </ToastProvider>
      </body>
    </html>
  );
}
